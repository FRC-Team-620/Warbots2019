##########################################################
#  This file was generated at one time - in order to ease
#  comparison with other config files it is advisable that
#  you do not alter the ORDER of things, though you're free
#  to change the values, comment-out, or remove names altogether
##########################################################
#Control Scheme (Scorer)

#A Button

# This is a binary control (like b, x, y, leftBumper, rightBumper, All the possible binary commands are listed below. 
#
# Options:
#   tazOpenCommand
#   tazStowCommand
scorer.A.pressed = 

# B Button (binary command)
#
# (no options defined)
scorer.B.pressed = 


# X Button (binary command)
#
# (no options defined)
scorer.X.pressed = 

# Y Button (binary command)
#
# (no options defined)
scorer.Y.pressed = 

# Left Bumper (binary command)
#
# (no options defined)
scorer.LeftBumper.pressed = 

# Right Bumper (binary command)
#
# (no options defined)
scorer.RightBumper.pressed = 

# Pressing DPad UP
#
# (no options defined)
scorer.pov.up = 

# Pressing DPad RIGHT
#
# (no options defined)
scorer.pov.right = 

# Pressing DPad DOWN
#
# (no options defined)
scorer.pov.down = 

# Pressing DPad LEFT
#
# (no options defined)
scorer.pov.left = 

#Left Stick 
scorer.LeftJS.Y = 
scorer.LeftJS.X = 
#Right Stick 
scorer.RightJS.Y = OI.elevator.speed
#scorer.RightJS.X = OI.robot.rotation_rate 
#Left Trigger
scorer.LeftTrigger = 
#Right Trigger
scorer.RightTrigger = 
#D-Pad Up
scorer.DPadUp = 
#D-Pad Down
scorer.DPadDown = 
#D-Pad Left
scorer.DPadLeft =
#D-Pad Right
scorer.DPadRight =
#Left Stick (Movement)
scorer.LeftAxis =
#Right Stick (Movement)
scorer.RightAxis =

#Other Variables
#Left Stick Deadzones
scorer.LeftDeadzoneX =
scorer.LeftDeadzoneY =

#Right Stick Deadzones
scorer.RightDeadzoneX = 
scorer.RightDeadzoneY = 

